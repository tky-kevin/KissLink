<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <EditText
            android:id="@+id/etFieldLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:textColor="@color/beam_muted"
            android:textSize="16sp"
            android:textFontWeight="600"
            android:letterSpacing="0.06"
            android:hint="@string/profile_field_label"
            android:textColorHint="@color/beam_muted"
            android:inputType="text"
            android:maxLines="1"
            android:enabled="false"
            tools:text="電話" />

        <EditText
            android:id="@+id/etFieldValue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:textColor="@color/beam_ink"
            android:textSize="12sp"
            android:hint="@string/profile_field_value"
            android:textColorHint="@color/beam_track"
            android:inputType="text"
            android:maxLines="1"
            android:enabled="false"
            tools:text="0912 345 678" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/ibRemoveField"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close"
        android:scaleType="centerInside"
        android:padding="8dp"
        android:visibility="gone"
        tools:visibility="visible" />
</LinearLayout>
